---
name: Gao Hongyu
url: https://gao.show
slug: gao-hongyu
category: portfolio
industry: awwwards
reachable: false
---

# Gao Hongyu

**One-liner:** Could not be verified live — `gao.show` does not currently resolve (DNS NXDOMAIN on both 8.8.8.8 and 1.1.1.1), so the "world-class" claim is unconfirmed by this teardown.

## Overview
The target domain `https://gao.show` was **unreachable** at the time of this teardown. Evidence:
- `nslookup gao.show` against Google DNS (8.8.8.8) → "Non-existent domain"
- `nslookup gao.show` against Cloudflare DNS (1.1.1.1) → "Non-existent domain"
- Headless browser navigation → `DNS_PROBE_FINISHED_NXDOMAIN`
- `WebFetch https://gao.show` → `ECONNREFUSED`
- Wayback Machine availability API → no archived snapshots (`archived_snapshots: {}`)
- RDAP lookup returned empty

So either the domain has expired/lapsed, was never registered, or the slug/URL is incorrect. No live DOM, snapshot, or screenshots could be captured. Everything below is **inference from web search only** and is explicitly flagged as unverified.

### Important accuracy caveat
A web search surfaced a Codrops case study ("Letting the Creative Process Shape a WebGL Portfolio", 2025-11-27) that an automated summary initially attributed to "Gao Hongyu." On fetching the actual article, it is about **Roman Jean-Elie (romanjeanelie.com)** — NOT Gao Hongyu. The dancing-character / WebGL-text-stretch / `back.out(1.2)` / camera-FOV details belong to that other portfolio and must NOT be credited to gao.show. They are excluded from the sections below to avoid contaminating the dataset with mis-attributed techniques.

No independent, reliable source describing `gao.show` / a designer "Gao Hongyu" as an Awwwards portfolio could be confirmed. Search results for the name returned unrelated people (academics, athletes, actors).

## Layout & Information Architecture
Unverified — site did not load. No structure could be observed.

## Navigation
Unverified — site did not load.

## Typography
Unverified — site did not load. (A computed-style font probe could not run because there was no page.)

## Color & Theme
Unverified — site did not load.

## Imagery / Media
Unverified — site did not load.

## Motion & Interaction
Unverified — site did not load. No motion claims can be made. (Note: any WebGL/dancing-character/scroll-velocity-stretch descriptions found in search belong to Roman Jean-Elie's portfolio, not this site — see caveat above.)

## Tech / Stack (if known)
Unknown / unverifiable. No DOM, headers, or bundle could be inspected because the domain does not resolve.

## Notable Techniques (steal-worthy)
- None can be responsibly listed for this specific site — it could not be loaded and no trustworthy secondary source describes it.

## What to steal for a personal portfolio
- Nothing verifiable from this target. Recommend re-confirming the correct URL (it may have moved TLD/domain, or the designer's site may live elsewhere) and re-running the teardown. If the intent was the WebGL "dancing character" portfolio seen on Codrops, the correct subject is **romanjeanelie.com** (Roman Jean-Elie), which should be torn down under its own slug rather than mis-filed here.

## Screenshots
- (none saved — only a browser `DNS_PROBE_FINISHED_NXDOMAIN` error page was captured, which was deleted as it contains no site content)
